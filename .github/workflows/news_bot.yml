name: 24/7 AI News Blogger

on:
  schedule:
    - cron: '*/15 * * * *' 
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        persist-credentials: true

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Restore Session
      run: |
        echo "${{ secrets.CLIENT_SECRETS_JSON || vars.CLIENT_SECRETS_JSON }}" > client_secrets.json
        echo "${{ secrets.TOKEN_PICKLE_BASE64 || vars.TOKEN_PICKLE_BASE64 }}" | base64 -d > token.pickle

    - name: Run News Bot
      env:
        # Try secrets first, then variables as backup
        GROQ_API_KEY_1: ${{ secrets.GROQ_API_KEY_1 || vars.GROQ_API_KEY_1 }}
        GROQ_API_KEY_2: ${{ secrets.GROQ_API_KEY_2 || vars.GROQ_API_KEY_2 }}
        BLOG_ID: ${{ secrets.BLOG_ID || vars.BLOG_ID }}
      run: |
        # Delete local .env if it exists to prevent overwriting
        rm -f .env
        python main.py

    - name: Save Progress
      run: |
        git config --global user.name 'github-actions'
        git config --global user.email 'github-actions@github.com'
        git add posted_news.txt last_key_index.txt || echo "No files"
        git commit -m "Sync progress [skip ci]" || echo "No changes"
        git push origin main
